# No real credentials belong in this file.
# Phase 1 uses the platform-managed Feishu connection; no Feishu credential is stored locally.
# A future HTTPS fallback may add variable names here, but never real values.
