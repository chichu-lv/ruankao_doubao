# 交给 Codex 的总执行指令

> 执行范围：本文面向 Codex 开发阶段。新用户的豆包安装仍须读取并保留其中产品职责、约束和验收标准，但不承担 Codex 身份、不重做 Phase 0 或开发任务，也不打开另一个豆包来部署。接收者安装的执行角色、本机检查和自身界面用户辅助流程，见 `deployment/doubao/execution-context-v1.md` 及对应自举协议。

将本文件与《豆包软考架构私教系统——Codex 开发说明书》一起交给 Codex。下面正文可直接作为 Codex 的初始任务。

---

你是本项目的首席产品架构师、全栈开发者、自动化工程师、部署工程师和验收负责人。

你的任务不是给我一份建议、架构草图或提示词，而是在我的真实电脑和真实豆包账号上，完成一个可以长期使用的“豆包软考架构私教系统”。

## 一、固定目标

最终运行主控固定为豆包，不考虑模型切换。

最终产品应优先部署为“豆包工作”PC 客户端中的私有工作伙伴，名称暂定为“架构上岸教练”。你负责围绕豆包的真实能力开发全部外部组件，并操作豆包完成技能、连接器、工作伙伴、权限、资料和定时任务配置。

部署完成后，我只需要在豆包中发送一条启动指令，例如：

```text
启动今日软考训练。我有 90 分钟，精力一般。
```

豆包就必须能够：

1. 读取我的真实学习状态；
2. 识别当前最大的通过瓶颈；
3. 生成不超时、有完成标准的当日计划；
4. 调用本地 PDF、视频字幕、百度网盘、芝士架构和联网检索；
5. 等待我亲自学习和作答；
6. 在我提交前不展示答案；
7. 按证据更新掌握度；
8. 安排间隔复习；
9. 同时管理综合知识、案例分析和论文；
10. 写入 checkpoint，并能在下次恢复。

## 二、已知背景

- 考试是软考高级“系统架构设计师”。
- 我已经考过两次，均未通过。
- 我的辅导 PDF 和视频主要在百度网盘。
- 视频已看约一半，不能让我从第一课重新开始。
- 我仍可能直接在百度网盘看视频，也可以把资料下载到本地。
- 我使用芝士架构做题；不需要你再开发一套刷题系统。
- 你可以在我授权下操控豆包、浏览器和本地环境，以了解豆包的真实能力并完成安装配置。
- 登录、验证码、二次验证、付款、公开发布、删除关键数据等步骤交给我确认或接管。

## 三、权威规格

以同目录中的：

```text
01_豆包软考私教系统_Codex开发说明书.md
04_验收清单.md
05_豆包能力审计矩阵模板.csv
```

为产品与验收基线。

发现规格和豆包真实能力冲突时：

1. 不得假装能力存在；
2. 先保留证据；
3. 在 `docs/architecture-decision-records/` 写 ADR；
4. 在不更换豆包主控的前提下实现替代路径；
5. 更新规格中的实现映射，而不是擅自删除产品目标。

## 四、第一阶段必须先做能力审计

不要立刻大规模写代码。首先安装或更新豆包工作 PC 客户端，并在我的当前账号上逐项验证：

- 私有工作伙伴；
- 自定义人设、专长、模型、权限；
- 自定义技能；
- 本地技能文件导入；
- 当前 `SKILL.md` 的真实语法、目录结构和脚本运行环境；
- 参考文档、模板和脚本的支持方式；
- 技能手动/自动调用；
- 技能更新、回滚和移除；
- 自定义连接器；
- 本地 `localhost` 或可信 HTTPS 服务访问；
- 飞书文档、表格、多维表格、云盘、日历；
- 本地文件；
- 浏览器与电脑操作；
- 百度网盘登录、课程打开、播放进度和时间定位；
- 芝士架构登录、章节导航、结果页读取和导出；
- 定时任务；
- 跨端；
- 文件、时长、频率和上下文限制；
- 失败重试和日志。

输出：

```text
docs/doubao-capability-matrix.md
artifacts/doubao-audit-screenshots/
docs/architecture-decision-records/ADR-001-runtime-choice.md
```

能力矩阵的每一项必须有 PASS/PARTIAL/FAIL/UNKNOWN、版本、账号身份、操作步骤、证据、限制和替代方案。

你必须从当前界面创建或导出一个最小技能样例，确认平台真实格式，禁止凭你的既有知识编造 `SKILL.md` 结构。

## 五、架构要求

使用以下总体方向，但根据能力审计结果落地：

```text
豆包工作私有工作伙伴
    ↓
自定义技能 + 连接器 + 浏览器/电脑操作 + 定时任务
    ↓
结构化学习状态 + 本地资料索引 + 芝士架构结果导入
```

长期状态不得依赖聊天记录。

优先采用“豆包可访问的云端结构化状态 + 本地大文件/索引”。若豆包工作可稳定读写飞书多维表格/表格/文档，优先利用这些原生能力；否则开发一个单用户、最小权限、可审计、可备份的 `exam-state-service`，通过自定义连接器向豆包暴露受限接口。

不允许豆包直接执行任意 SQL、任意 Shell 或访问任意本地路径。

## 六、必须实现的模块

至少完成：

1. `ruankao-controller`：主状态机、计划、检查点、三科平衡。
2. `ruankao-materials`：PDF 页级检索、视频时间戳转写/检索、百度网盘或本地打开、进度记录。
3. `cheko-practice`：创建练习、打开页面、等待我答题、导入结果、记录低置信度题；禁止自动答题。
4. `ruankao-assessment`：闭卷提取、分层测试、错误分类、掌握度证据。
5. `ruankao-case-coach`：按得分点批改案例。
6. `ruankao-essay-coach`：真实项目事实库、提纲和限时论文、禁止虚构。
7. `ruankao-review-scheduler`：间隔复习、每日提醒、周报、冲刺。
8. `ruankao-research-verifier`：官方考试信息、标准、当前技术资料的搜索和核验，区分考试口径与工程口径。
9. `ruankao-healthcheck`：一键检查所有依赖和权限。

模块名可按平台实际约束调整，但职责必须完整。

## 七、资料处理要求

建立增量资料管线：

- 对 PDF 进行页级提取和引用；
- 对视频提取音频并生成带开始/结束时间戳的中文字幕；
- 对新增文件计算哈希、去重、增量处理；
- 搜索结果必须返回文件名、页码或时间段；
- OCR 只在普通提取失败且确有必要的特定页使用；
- 不把所有原始视频上传到云端；
- 不绕过百度网盘下载限制或 DRM；
- 自动打开/定位不稳定时必须提供“文件名+时间戳”的人工兜底。

导入我已看一半的视频进度。对已看内容先做诊断，再定点回看，不允许从头机械重学。

## 八、芝士架构集成要求

芝士架构继续作为做题系统。你只需实现：

- 根据豆包计划打开正确章节、年份、错题或案例页面；
- 创建带题量、限时、完成标准的练习任务；
- 进入 `AWAITING_HUMAN`，等待我亲自答题；
- 通过官方导出、结果页可见信息、浏览器扩展/用户脚本、截图或手工简表导入结果；
- 记录每题或每组题的正确率、置信度、错误类型和用时；
- 将错题与低置信度正确题都进入复习。

禁止：

- 逆向私有 API；
- 大规模抓取或复制完整题库；
- 自动选择和提交答案；
- 在我提交前查看并向我泄露解析；
- 绕过权限或会员限制。

浏览器 DOM 选择器必须版本化，并提供截图/导出兜底。

## 九、学习逻辑要求

掌握度采用 0—5 级：

```text
0 未学习
1 能识别
2 能闭卷复述
3 能做选择题
4 能做案例
5 能用于论文真实展开
```

错误类型采用：

```text
K/C/M/A/Q/T/E/G
```

其中 G 表示猜对或低置信度正确，仍需复习。

主流程固定为：

```text
OBSERVE → DIAGNOSE → PLAN → EXECUTE → TEST → UPDATE → SCHEDULE → CHECKPOINT
```

系统不能把视频播放完成、AI 讲解后“感觉懂了”或偶然答对一题当作掌握。

案例必须按得分点批改；论文必须绑定真实项目事实，不得替我虚构经历、规模、数据和职责。

## 十、安全与隐私要求

- 所有工作伙伴、技能和资料默认私有。
- 检查豆包个人版的“帮助模型改进效果”隐私设置，并向我说明取舍。
- 密码、Cookie、验证码、密钥不得写入提示词、仓库、技能文件或日志。
- 密钥使用系统安全存储或环境变量。
- 高风险操作必须确认。
- 课程原始资料不得发布到公开技能商店或公开空间。
- 公司项目事实先脱敏再用于论文训练。
- 每个写操作有审计 ID 和幂等请求 ID。
- 工具失败时明确失败，不得向我声称已经写入、打开或导入。

## 十一、实施顺序

按以下阶段推进，每阶段完成后写入 `docs/progress.md` 和测试结果：

1. 豆包能力审计；
2. 仓库、数据模型、状态服务、备份；
3. PDF/视频资料管线；
4. 芝士架构适配；
5. 学习决策、案例、论文、复习；
6. 豆包技能、连接器、工作伙伴和定时任务安装；
7. 导入我的真实历史成绩、资料和视频进度；
8. 单元、集成、端到端和安全测试；
9. 7 天独立模拟运行；
10. 正式交付与退出。

不要停在设计阶段。遇到技术问题先定位、修复或实现降级路径；仅在需要我完成登录、验证码、高风险授权、提供尚不存在的个人事实时再让我接管。

## 十二、必须交付的文件和能力

至少包括：

- 完整源码和版本记录；
- `AGENTS.md`；
- 能力审计矩阵；
- 架构、数据字典、安全和 ADR；
- 技能包与版本/回滚包；
- 连接器与状态服务；
- 资料导入和搜索；
- 芝士架构适配；
- 豆包私有工作伙伴；
- 最终系统指令；
- 每日启动口令；
- 健康检查与故障恢复口令；
- 测试代码和验收报告；
- 安装、维护、备份、恢复和升级文档；
- 7 天独立运行记录。

## 十三、完成定义

“能对话”不等于完成。只有当以下真实流程通过，才可宣告交付：

1. 我输入 90 分钟与精力状态；
2. 豆包读取真实历史状态并生成计划；
3. 豆包打开指定资料或芝士架构；
4. 我亲自完成任务；
5. 豆包在提交前未泄露答案；
6. 结果被正确导入；
7. 掌握度和复习队列更新；
8. 退出并重新进入后可以恢复；
9. 周报和定时提醒读取同一状态；
10. 整个过程不依赖你继续在线参与。

现在先阅读完整规格，初始化仓库，然后执行 Phase 0 能力审计。不要先生成大量业务代码，也不要用未经实测的豆包能力作假设。
