# 最终交付验收：Windows 新用户

日期：2026-09-05（Asia/Shanghai）。状态：**NOT_RELEASED / WAITING_FOR_AUTHORIZATION**。

用户指定接收者使用 Windows。Mac 或模拟测试不能替代 Windows 通过证据；本记录没有宣称七天真实独立运行通过。

## 当前版本

- main 已推送至 `50202f7`（1.1.1）。此前修复提交 `08d7e92`、`42cb92e`。
- `dist/offline/architectpass-offline-1.1.1.zip` 是早期候选，只含 `08d7e92` 与 Mac 运行环境，不是最终 Windows 交付包。
- Windows 官方 Python 3.12.10 x64 嵌入式运行环境及 8 个依赖 wheel 已下载到被 Git 忽略的 vendor 目录；Windows 启动器已实现，但尚未取得真实 Windows 运行结果。
- 未生成独立 SHA 文件。最终 Windows 包尚未重建和放行。

## 已通过

| 范围 | 本次证据 | 结论 |
|---|---|---|
| 完整资料 | 指定两目录共 349 文件：121 MP4、209 PDF、16 DOCX、1 RAR、1 PNG、1 空标记文件 | 下载清单及大小齐全 |
| PDF | 209 PDF 共 8,128 页均能打开，每份首屏渲染成功 | PASS |
| 视频 | 121 视频均检出视频轨与正时长 | PASS；未逐帧观看全部课程 |
| 回归测试 | 本地运行 unittest，94 项通过 | PASS（Mac） |
| 七天加速模拟 | 19 项检查全部通过，7 个逻辑日 checkpoint，离线补写、复习、三科、周报、备份 | PASS（模拟；无外部服务调用） |
| 无 Git 解压目录 | 从实际 26GB 候选 ZIP 解压至独立临时目录 | 实际文件可读 |
| 豆包本地启动 | 豆包实际执行 Mac 包内启动器；Python 3.12.13、bundled_wheels | 本地必需检查无 FAIL；在线/OCR/私有旧索引检查明确 PARTIAL |
| 豆包资料初始化 | 349 文件登记，首批 2 PDF、44 页索引；检索得到页码 6/8 和当前绝对路径 | PASS |
| 新飞书 Base | 单独创建 `ArchitectPass Acceptance 0905`，15 张表 | PASS（豆包执行、回读） |
| 空档案初始化 | 画像 1、知识点 10、资源 2、审计 13；未继承旧用户成绩或学习进度 | PASS |
| 初始化重放 | 画像/知识点/资源/审计均新增 0、更新 0；topics 10/10 主键唯一 | PASS（真实回读） |

## 本次发现与修复

1. 离线说明错误地转入 Git/百度网盘流程：增加专用离线启动协议和入口提示词，明确包内资料无需外部网盘。
2. 新用户继承原用户视频进度和历史结果：初始化改为空档案，清除默认学习事件/观看证据，并将职业背景设为未知。
3. 私有片段构建依赖固定目录、固定 49 片段：支持实际提供的 catalog、实际片段数及对应视频来源。
4. 新机器缺少 Python/依赖：加入 Mac 便携环境，以及待 Windows 实测的官方嵌入式环境和离线 wheel 启动流程。
5. Windows 中文编码及 Mac 临时目录写死：Python 子进程显式 UTF-8，移除 macOS 专有 pycache 路径。
6. 豆包真实写入错误：误解析飞书 CLI 结果后重试，造成 10 个重复 topics。豆包自行识别并移除其误写批次，回读复验为 10 个唯一知识点。增加 `deployment/feishu/write-protocol-v1.md`：识别 `data.fields`/`data.data`，状态未知不等于空表，稳定请求 ID + 业务主键查重，部分成功仅补缺失行。之后真实重放零新增、零更新。此项是安装协议修复与真实回归，并非新增服务端唯一约束。
7. Base 名写死：主控、健康检查和系统指令先读取实际项目绑定，测试 Base 不应漂移到正式 Base。
8. 芝士架构实际进入 `/anti-hack`「检测到调试窗口」：记录为自动读取受限，停止自动化，使用官方界面/截图/人工简表；不绕过访问限制，本地 PDF 训练可继续。

## 尚未完成，不能算 PASS

- **Windows 实测结果**：已向私有仓库推送只读 GitHub Actions 工作流。Edge 的 Actions 页面反复空白，未能读取运行日志；未宣称已成功执行。拟增加专用验收分支写入日志的工作流，被自动权限审查拒绝（contents:write 扩大权限）。变更仅留在本地、未提交未推送；已请求用户明确授权。
- **豆包真实项目创建/绑定**：豆包已准备本地 `project-state.json`，但真正操作客户端时停在“操作电脑需要系统授权”。仅有配置文件不等于项目已创建。打开授权提示的操作也被自动审查拦截，未绕过。
- 因上述项目绑定步骤暂停，本次新项目中的首次学习 checkpoint、独立新会话恢复及完整一键安装尚未完成。
- 原 Git 启动协议在 Windows 无系统 Python/Git 情况下的端到端路径仍须实测；不能用离线 ZIP 的预置环境替代此项。
- 实际新账号注册、真实七天独立运行、Windows 豆包 GUI 安装和自然时间触发的提醒，未在本次验证。

## 本地证据位置

- 仓库 `dist/offline/real-materials-test.json`：全量资料文件探针。
- 独立解压目录 `project/dist/bootstrap/local-bootstrap-result.json`：豆包运行本地启动器结果。
- 同目录 `project/dist/acceptance/topics-list-raw.json` 与 `replay-summary.json`：真实飞书回读和零差异重放。后者的时间字段由豆包生成，本报告只采用其计数与记录内容，不作为独立时钟证据。
- 豆包实际测试会话：`架构上岸教练部署与验收`。测试范围未写入原正式学习 Base。

## 放行条件

取得真实 Windows 执行日志并修复所有失败；完成豆包系统授权后的真实项目创建、训练 checkpoint 与新会话恢复；从最终干净 main 重新生成包含 Windows runtime 的完整 ZIP，并验证实际包。完成前不建议向新用户交付旧候选包。
