# 干净用户部署重测：清理门禁

日期：2026-09-05 至 2026-09-06。代码 main dec2d0c / 1.1.4。

用户要求移除本项目全部豆包残留，以初始公开提示词和普通用户授权/点击完成部署；出现需开发者指导的卡点即记录失败，修复后重置再测，不把已有环境恢复算作新用户部署。

## 已完成（真实 UI 回读）

- 删除两个项目：架构上岸教练、架构上岸教练-交付验收0905。最终不可撤销操作经用户明确确认。
- 删除九个产品技能与三个 Phase0 技能，技能数量 109 → 97，专用条目消失。
- 删除 ArchitectPass Phase0 Local Probe 连接器，数量 2 → 1，仅剩共用百度网盘。
- 删除正式每周复盘与三个 Phase0 定时任务，均已不在列表。
- 删除 18 条本项目部署、体验和审计对话。ArchitectPass 与架构上岸的半年内消息检索无结果；中文搜索加载失败经重试后才确认。
- 默认本机部署绑定移至忽略提交的隔离证据目录，默认位置为空。开发仓库、课程、最终包未删除，不给新任务作为安装依赖。

保留无关 AI Tutor/pass_ai、主对话和其它数据。技能可以重新导入；项目/定时任务界面提示不可恢复。对话删除曾提供短暂撤销按钮，不保证长期可恢复。

## 后续确认与清理完成

- 用户再次确认精确五个云端文件的删除和临时关闭记忆。
- 两个 State v1、Acceptance 0905、Phase0 State Probe 删除后逐个回读「该文档已被删除」。Phase0 Doc Probe 通过云盘目标行删除，确认框说明移入回收站、30 天后自动彻底删除；未清空回收站。
- 最终云盘 ArchitectPass 搜索返回「未搜索到相关结果」。
- 自动记忆从 on 切为 off 并回读。测试结束须恢复 on。这是隔离，不是证明平台隐藏历史记忆已销毁。
- 关闭本项目五个工作台标签和两个浏览器标签；未关闭其它项目。
- 本机飞书客户端是不同的文件视图，不能代替豆包云盘证据。

## 第一轮公开入口实测

00:52 左右从桌面空白工作任务发出 main 1.1.4 README 提示词，只将 Windows 改为真实平台 Mac；发送前确认「本地电脑」，未选旧项目、未附旧文件、未给开发路径。普通用户操作由 Codex 等价执行，技术部署由豆包执行。

豆包首轮表示检查本机与网盘、读取部署文档。首次阅读的工具摘要出现 ruankou_doubao（非用户给出的 ruankao_doubao），继续观察是否自行纠正，不由测试操作员提示。

后续自行纠正了仓库名，但把自身输入错误归因为用户链接错误，记录为缺陷。unzip 中文文件名解压失败后自行改用 Python zipfile；仅申请一次清除本轮失败解压子目录的用户授权，观察员点击允许一次。之后完成基线读取、版本/文件核对和项目私有构建，报告九个技能 ZIP 生成及六项本地检查 PASS/PARTIAL。又使用已有共用网盘连接器定位两个授权目录。尚未验证项目/技能安装、状态库或完整健康检查；生成的 ZIP 不等于安装成功。

## 第一轮结论：FAIL（不能交付为初始提示词＋授权）

- 豆包正确处理自身 GUI 不可用，没有进入第二个豆包/虚拟桌面登录。自行建成新状态库并报告 15 表、13 业务记录及 13 审计记录，幂等重放无新增。
- 随后要求用户创建项目、逐个导入 9 个技能、绑定系统指令。严格的「仅初始提示词＋授权」已不满足；作为宽松普通用户点击路径继续检查。
- 用户等价操作创建了新「架构上岸教练」项目。实际创建界面没有豆包声称的可见性选项，未将其记为已配置。
- 经「技能 · 连接器 → 新建 → 上传技能 → 选择文件」导入本轮新目录的主控 ZIP，个人分类显示启用。没有使用开发目录旧 ZIP。
- 同一列表另有启用的 Ruankao Review Scheduler V1，观察员本轮没有导入；来源未证实，清理完整性因此降为未确认，不能将本轮视为完全干净环境。
- 项目编辑界面仅有名称与本地文件夹，既没有项目指令字段，也没有单独附加项目上下文的入口，豆包提供的第三步无法按文字完成。
- 停止本轮验收计分。开始单独的只读开发核查，验证原生创建技能能否正式安装，以及个人/本地技能来源；该核查不算新用户行为。

状态：ATTEMPT_1_FAILED / DEVELOPER_INVESTIGATION。下一轮前须再次清理本轮新项目、主控/复习技能、新状态库、对话及绑定，并在新的完整清理证据后重测。自动记忆仍为临时 off，测试工作结束须恢复。

这是同账号项目清理后的测试，不等于全新注册账号；共享网盘登录、系统已有工具和权限仍在。尚无证据支持完整部署通过。本地详细流水保存在 dist/qa/clean-user-deployment-2026-09-05.md。

## 第二轮开始（main 1.1.5 / 1707c25）

按 ADR-009 完成官方文件夹发现探针验证后发布候选。第二轮前再次删除第一轮主控/复习与开发探针技能，实际官方技能目录精确核对无项目文件，界面无项目技能且总数 97。删除第一轮状态库，最终 ArchitectPass 云盘查询无结果；删除第一轮项目、部署对话、技能咨询与独立探针对话，侧栏回读均消失。第一轮 106 MB 安装移入忽略提交的证据隔离区，原位置为空。

明确点击「新工作任务」，核对空白 URL、本地电脑和无工作台旧附件，发送 1.1.5 README 原始启动提示词，未补开发路径。状态：ATTEMPT_2_RUNNING。自动记忆保持临时 off，测试结束须恢复；尚未判通过。
