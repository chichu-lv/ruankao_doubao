# 豆包部署误入虚拟桌面登录：入口修复

日期：2026-09-05。main：dec2d0c / 1.1.4。

状态：协议与本地回归 PASS；接收者实际恢复待反馈，纯提示词端到端部署未通过验证。

## 问题与证据

用户报告已交付的新用户没有报错，但豆包一直在虚拟桌面等待用户登录豆包。本轮未取得该设备截图或工具轨迹，不能断定任务本身处于云模式，还是本地任务调用了隔离浏览器。

只读核对发现：公开和离线自举协议均允许在无法操作桌面自身界面时，通过豆包官方网页创建项目；强制读取的 02 开发执行指令也未清楚区分开发者任务与接收者安装任务。两者存在引导重复打开豆包的路径。之前真实验收有人工等价模式/项目点击和故障指导，不能作为单提示词全自动安装证据。

## 已修复

- 新增 execution-context-v1.md：明确当前执行者就是豆包而非 Codex；本机目标确认 CONTEXT_CHECK；NEEDS_LOCAL_MODE 与 NEEDS_USER_UI 分开处理。
- 移除公开和离线协议中网页豆包创建项目的兜底；禁止嵌套豆包会话与虚拟桌面豆包登录。不能操作自身界面时仅请求明确用户点击，不绕过平台限制。
- 02 保留完整产品职责与验收要求，添加开发阶段适用范围；03 与生成后的系统指令一致添加当前身份及环境边界。
- README、新生成离线包的提示词和中断恢复均保留已确认属于本次部署的项目、绑定和学习状态，不盲目重建。
- 旧离线包和 Gitee 镜像不会自动更新；给用户可直接粘贴、无需下载即可先停止错误路径的恢复提示词。既有 1.1.1 全资料 ZIP 未重建或修改。

## 验证与边界

- 102 项本地 unittest 全通过，包括新增身份/环境协议、可复制提示词、离线 README 和源码包含共同协议的回归断言。
- phase5_healthcheck 实际重新生成系统指令、构建九技能并通过专项测试；证明生成器不会抹去身份修复。
- git diff --check 通过；主分支推送成功。无真实账号、项目、技能、飞书数据或用户资料变更；未生成独立 SHA 文件。
- 这些是文本契约、构建和后端回归，不证明豆包必然遵循提示词。没有在接收者电脑完成真实恢复，不宣称登录等待已解除或安装完成。
- 用户即时操作：停止等待任务，不在虚拟桌面登录；原任务发送纠偏提示词。仅在工具确实不能访问本机时，另开桌面本地任务，保留原链接和已完成步骤，从本机核对恢复。
