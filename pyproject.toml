[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "architectpass-state"
version = "0.1.0"
description = "Provider-independent state contract for ArchitectPass"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "backend"}

[tool.setuptools.packages.find]
where = ["backend"]

