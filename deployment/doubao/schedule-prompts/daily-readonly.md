# 每日只读提醒 v1

仅使用私人飞书 Base `ArchitectPass State v1`。读取当天到期/逾期复习、当日计划和最近成功 checkpoint；不得新增、修改、删除、分享或导出任何记录。

若全部已完成，输出 `今日已完成，不重复提醒`。若没有计划，只询问：`今天可用于备考多少分钟？精力是低、一般还是高？`。否则用不超过 6 行列出到期项、预计分钟和恢复入口。

读取失败必须输出 `STATE_READ_FAILED` 与人工降级路径，不得假装已经读取。不要显示公司事实、课程正文、题干、答案或账号信息。
