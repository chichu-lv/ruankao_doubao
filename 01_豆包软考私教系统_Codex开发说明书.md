# 豆包软考架构私教系统——Codex 开发说明书

版本：0.1  
日期：2026-09-03  
产品代号：`ArchitectPass / 架构上岸教练`  
最终运行主控：豆包  
目标载体：豆包工作 PC 客户端中的私有工作伙伴  
开发、配置、迁移、测试与交付：Codex

---

## 1. 文档目的

本说明书用于指导 Codex 设计并真正实现一套以豆包为最终主控的“系统架构设计师备考操作系统”。

系统应让豆包不再只是被动答疑工具，而成为一个可以持续读取学习状态、制定计划、调用资料和网页工具、监督用户做题、评估掌握度、安排复习、批改案例与论文、检索权威信息并追踪通过概率的备考主控。

Codex 不得只输出架构图、提示词或演示原型。Codex 的任务结束条件是：系统在用户真实豆包账号和真实电脑上完成部署，关键流程通过端到端测试，用户无需继续让 Codex参与日常学习。

---

## 2. 已知用户背景与初始约束

### 2.1 考试与学习状态

- 考试：软考高级“系统架构设计师”。
- 用户过去已参加两次考试，均未通过。
- 用户已有辅导机构课程，PDF 和视频主要存放于百度网盘。
- 视频已经观看约一半，不能从第一节机械重学。
- 视频主要由辅导老师讲解，用户会继续在百度网盘或本地播放器观看。
- 做题平台固定为芝士架构，不开发新的刷题网页或完整题库。
- 需要同时覆盖综合知识、案例分析、论文三条线，目标是一次通过全部科目，而不是只提高选择题正确率。

### 2.2 产品与技术约束

- 最终 AI 主控固定为豆包，不考虑模型切换。
- Codex 可在用户授权下操作豆包、浏览器、本地文件和相关账户界面，用于能力审计、安装、配置与测试。
- 登录、验证码、二次验证、付款、公开发布、删除关键数据等动作必须由用户确认或接管。
- 课程资料可能受版权约束，只做个人备考用途，不公开分发，不绕过下载限制、DRM、付费墙或平台权限。
- 芝士架构未确认存在面向个人开发者的公开 API，因此默认通过浏览器可见操作、官方导出、截图/文件导入等方式集成。

---

## 3. 产品目标

### 3.1 核心目标

让豆包能够围绕“通过系统架构设计师考试”持续执行以下闭环：

```text
读取状态
→ 识别当前最大通过瓶颈
→ 生成受时间与精力约束的学习计划
→ 调用资料、视频、芝士架构或联网检索
→ 等待用户真正完成学习/作答
→ 闭卷测试或按得分点批改
→ 写入学习证据
→ 更新掌握度与风险
→ 安排间隔复习
→ 生成可恢复的会话检查点
```

### 3.2 用户体验目标

部署完成后，用户日常只需输入类似：

```text
启动今日软考训练。我有 90 分钟，精力一般。
```

豆包应自动完成：

1. 读取最新学习状态与到期复习；
2. 判断今天最应该做什么，而不是机械续播课程；
3. 给出总时长不超限、每项有完成标准的计划；
4. 必要时打开指定 PDF、视频、百度网盘页面或芝士架构章节；
5. 用户提交答案前不展示答案；
6. 训练完成后更新数据库并安排复习；
7. 明确告诉用户今天完成了什么、薄弱点发生了什么变化、下一次要做什么。

### 3.3 可量化成功指标

系统上线后至少追踪：

- 综合知识：限时正确率、低置信度正确率、章节覆盖率、重复错误率、整套题趋势。
- 案例分析：得分点覆盖率、无关内容率、表达完整度、限时完成率、题型迁移能力。
- 论文：提纲完成率、完整成文次数、切题率、项目事实完整度、技术决策与权衡覆盖、限时完成率。
- 学习过程：计划完成率、到期复习完成率、视频“观看→闭卷复述→做题转化”率、连续学习天数。
- 系统质量：状态写入成功率、资料检索可追溯率、浏览器操作成功率、失败可恢复率、答案泄露事故数。

验收时不要求模型准确预测最终分数，但必须能基于证据给出风险趋势，禁止用没有数据依据的“通过概率”制造虚假确定性。

---

## 4. 最终产品形态

### 4.1 首选载体

首选使用“豆包工作”独立 PC 客户端，创建一个仅用户本人可见的工作伙伴：

```text
名称：架构上岸教练
身份：系统架构设计师一对一备考主控
可调用：自定义技能、连接器、浏览器/电脑操作、飞书文档/表格/多维表格/日历、定时任务
```

选择该载体的原因：当前官方资料显示，豆包工作支持多步骤 Agent 任务、本地文件与网页读取、浏览器和部分电脑界面操作、定时任务、自定义技能、连接器以及可配置工作伙伴；独立 PC 客户端更适合处理本地文件和长程任务。

### 4.2 普通豆包对话的定位

普通豆包聊天窗口不作为唯一系统承载层，因为普通对话不等于：

- 已安装自定义技能；
- 已连接持久状态库；
- 已获得本地目录权限；
- 已具备芝士架构浏览器流程；
- 已配置周期任务；
- 已获得可审计的长期学习状态。

普通豆包可作为移动端入口、临时答疑入口或跳转到豆包工作的入口，但核心运行环境应由能力审计结果决定。

### 4.3 “一条指令”的准确含义

最终交付的“一条指令”是部署后的启动命令，不是安装命令。

Codex 必须先完成：

- 工作伙伴创建；
- 人设与系统规则写入；
- 技能安装；
- 连接器授权；
- 状态数据库建立；
- 资料导入与索引；
- 芝士架构流程配置；
- 定时任务配置；
- 端到端测试。

完成后，用户再发送一条启动指令，由豆包读取现有系统并开始服务。

---

## 5. 角色与职责边界

### 5.1 Codex：开发、部署与验收执行者

Codex 负责：

- 调研真实豆包版本和账号能力；
- 设计架构与数据模型；
- 开发本地资料处理程序、状态服务、连接器和辅助脚本；
- 编写豆包可安装的技能文件、执行指令、脚本、参考资料与模板；
- 操作豆包完成工作伙伴、技能、连接器和定时任务配置；
- 导入初始资料与进度；
- 执行单元、集成、端到端和回归测试；
- 提供备份、恢复、升级和用户手册；
- 发现豆包能力不足时，在仍以豆包为最终主控的前提下开发外部补充能力。

Codex 不负责日常长期备考主控；上线后应退出运行链路。

### 5.2 豆包：最终备考主控

豆包负责：

- 每次训练前读取状态；
- 诊断薄弱点并计划；
- 调用技能、连接器、浏览器和资料；
- 组织学习、测试、复盘和复习；
- 维护三科平衡；
- 批改案例和论文；
- 进行联网检索和事实核验；
- 写入结构化学习证据；
- 通过定时任务提醒并生成周报。

豆包不得在运行期自行重构系统、随意改数据库结构、自动替用户答题或公开用户资料。

### 5.3 用户：学习执行者与高风险操作确认者

用户负责：

- 提供真实学习时间、精力和考试目标；
- 亲自观看课程、回忆、答题和写作；
- 对登录、验证码、二次验证、敏感授权和不可逆操作进行接管或确认；
- 对自己的项目经历、论文事实和成绩数据负责；
- 对 AI 批改中的可疑结论进行复核。

### 5.4 百度网盘：原始资料来源

- 保存和播放用户有权使用的课程资料；
- 可保留用户既有观看习惯；
- 不作为唯一知识索引或唯一进度数据库；
- 优先将可合法下载的 PDF、视频同步到本地资料目录。

### 5.5 芝士架构：外部做题引擎

- 展示真题、章节练习、案例与论文相关内容；
- 保存平台内做题记录；
- 提供错题、解析、统计、预测分和导出能力；
- 不由本项目复制成自建题库；
- 豆包只负责任务编排、打开页面、等待用户作答、导入结果和复盘。

---

## 6. 总体架构

```text
┌─────────────────────────────────────────────┐
│ 豆包工作 PC：私有工作伙伴“架构上岸教练”      │
│ 人设 / 计划 / 决策 / 对话 / 工具编排          │
└──────────────────────┬──────────────────────┘
                       │
        ┌──────────────┼────────────────┐
        │              │                │
        ▼              ▼                ▼
  自定义技能层      连接器层       原生豆包工作能力
  - 主控技能        - 状态服务     - 浏览器/电脑操作
  - 资料技能        - 本地桥接     - 飞书文档/表格
  - 做题技能        - 可选云服务   - 日历/定时任务
  - 评估技能                       - 联网研究
  - 案例/论文
        │              │                │
        └──────────────┼────────────────┘
                       ▼
┌─────────────────────────────────────────────┐
│                可审计数据与资料层             │
│ 状态库 / 事件日志 / 掌握度 / 复习队列 / 计划   │
│ PDF页级索引 / 视频时间戳字幕 / 案例 / 论文     │
└───────────────┬─────────────────┬───────────┘
                │                 │
                ▼                 ▼
        百度网盘/本地资料       芝士架构 Web
        下载、转写、检索        人工答题、结果导入
```

### 6.1 架构原则

1. **豆包是决策层，不是唯一存储层。**
2. **学习状态以结构化证据为准，不以聊天印象为准。**
3. **本地大文件与云端状态分离。** 课程视频/PDF 可保留本地，云端只保存必要元数据、索引和用户主动选择的片段。
4. **人在回路中。** 所有学习作答由用户完成；登录、验证码和高风险操作由用户确认。
5. **来源可追溯。** 重要结论必须能定位到教材页码、视频时间戳、题号或网页来源。
6. **能力检测优先。** 任何豆包集成方案都必须先在当前版本上验证。
7. **允许降级但不换主控。** 例如浏览器自动提取失败时，可改为截图/导出导入；最终仍由豆包管理流程。

---

## 7. 强制阶段 0：豆包实际能力审计

Codex 开始正式开发前，必须先操作用户当前豆包账号完成能力审计。不能根据宣传文档直接写死方案。

### 7.1 审计内容

至少测试：

- 豆包工作独立 PC 客户端是否可安装、登录、正常使用；
- 当前登录身份是豆包个人账号还是飞书账号；
- 当前客户端版本、订阅权益、实际可选模型；
- 是否能创建私有工作伙伴；
- 工作伙伴是否能设置人设、专长、模型、权限、技能和连接器；
- 是否能新建自定义技能；
- 是否能上传本地技能文件；
- 当前 `SKILL.md` 的真实格式、字段、目录结构、脚本运行环境和限制；
- 是否能导入脚本、参考文档、模板；
- 技能是否能手动调用与自动匹配；
- 技能更新、回滚、移除、版本显示是否可用；
- 自定义连接器的协议、认证方式、网络限制和读写权限；
- 是否能调用本机 `localhost`；若不能，是否能调用可信 HTTPS 服务；
- 是否能读写飞书文档、表格、多维表格、日历和云盘；
- 是否能读指定本地文件与目录；
- 是否能操作浏览器和电脑界面；
- 是否能登录、打开并导航百度网盘和芝士架构；
- 遇到登录/验证码时能否平滑交给用户接管；
- 是否能读取芝士架构当前页面的可见统计；
- 是否能创建一次性、每日、每周定时任务；
- 定时任务是否能访问同一状态层；
- 跨端查看任务和结果的实际效果；
- 文件大小、文件数、上下文、单次任务时长、调用频率等限制；
- 失败时的错误信息、重试方式和日志可见性。

### 7.2 审计输出

Codex 必须生成：

```text
docs/doubao-capability-matrix.md
artifacts/doubao-audit-screenshots/
docs/architecture-decision-records/ADR-001-runtime-choice.md
```

每项能力标记：

```text
PASS：原生满足
PARTIAL：可用但有限制
FAIL：不可用
UNKNOWN：尚未验证
```

每项记录：

- 测试日期；
- 客户端版本；
- 登录身份；
- 操作步骤；
- 实际结果；
- 截图/日志；
- 对架构的影响；
- 替代方案。

### 7.3 决策门槛

只有以下能力有可行路径后，才能进入正式实现：

- 私有工作伙伴或等价可持久配置入口；
- 至少一种自定义技能/工作流承载方式；
- 至少一种可持久读写的状态存储；
- 至少一种资料检索方式；
- 至少一种芝士架构结果导入方式；
- 至少一种定时提醒方式。

若豆包工作个人身份缺少某项原生能力，Codex 应优先用自定义连接器和外部状态服务补齐；仍然以豆包作为用户面对的唯一主控。

---

## 8. 状态与存储设计

### 8.1 推荐存储拓扑

采用“云端结构化状态 + 本地资料与缓存”的混合结构：

```text
云端/豆包可访问：
- 用户画像
- 考试配置
- 知识点树
- 学习事件
- 掌握度
- 复习队列
- 计划与报告
- 资料目录及引用定位

本地：
- 原始 PDF
- 原始视频
- 音频/字幕
- 页级文本
- 向量/全文索引
- 浏览器导出文件
- 本地备份
```

优先级：

1. 若当前豆包工作身份可稳定读写飞书多维表格、表格、文档与云盘，则优先使用其作为豆包可直接访问的结构化状态层。
2. 若多维表格或脚本能力不足，则 Codex 开发 `exam-state-service`，以 HTTPS 自定义连接器暴露严格受限的状态 API；后端可使用 SQLite/PostgreSQL，但必须有备份与迁移脚本。
3. 本地 SQLite 仅可作为缓存或离线备份，除非审计证明豆包与云端定时任务能稳定访问本机服务。

### 8.2 核心实体

至少包含以下逻辑表：

#### `user_profile`

- `user_id`
- `target_exam`
- `target_exam_date`
- `timezone`
- `weekday_minutes`
- `weekend_minutes`
- `past_exam_scores`
- `preferred_study_time`
- `current_video_progress`
- `constraints`

#### `exam_config`

- `exam_name`
- `exam_year`
- `official_syllabus_version`
- `subjects`
- `pass_rule`
- `exam_mode`
- `official_sources`
- `verified_at`

考试日期、科目和规则不得永久硬编码；上线和大版本升级时必须从官方来源核验。

#### `topics`

- `topic_id`
- `parent_id`
- `name`
- `syllabus_weight`
- `choice_relevance`
- `case_relevance`
- `essay_relevance`
- `prerequisites`
- `source_refs`

#### `resources`

- `resource_id`
- `type`：PDF / video / transcript / web / cheko / note
- `title`
- `local_path_or_uri`
- `copyright_scope`
- `processing_status`
- `checksum`
- `created_at`

#### `resource_segments`

- `segment_id`
- `resource_id`
- `page_start/page_end` 或 `time_start/time_end`
- `section`
- `text`
- `keywords`
- `topic_ids`
- `citation_anchor`

#### `video_progress`

- `video_id`
- `watched_until`
- `status`
- `last_watched_at`
- `recall_checked`
- `practice_checked`
- `needs_rewatch`

视频状态至少区分：

```text
未看
已播放但未检测
能闭卷复述
已转化为选择题能力
已转化为案例/论文能力
需要定点回看
```

#### `study_sessions`

- `session_id`
- `start/end`
- `available_minutes`
- `energy_level`
- `plan_id`
- `status`
- `checkpoint`

#### `practice_attempts`

- `attempt_id`
- `platform`
- `question_or_set_id`
- `topic_ids`
- `correct`
- `confidence`
- `duration`
- `error_type`
- `source_evidence`
- `submitted_at`

#### `mastery_evidence`

- `evidence_id`
- `topic_id`
- `evidence_type`
- `score`
- `confidence`
- `difficulty`
- `timed`
- `source_id`
- `created_at`
- `expires_or_decay_rule`

#### `mastery_state`

- `topic_id`
- `level_0_to_5`
- `confidence`
- `last_verified_at`
- `next_review_at`
- `risk_flags`

#### `review_queue`

- `review_id`
- `topic_id`
- `due_at`
- `review_type`
- `priority`
- `reason`
- `status`

#### `case_attempts`

- `case_id`
- `question_source`
- `user_answer`
- `rubric`
- `covered_points`
- `missing_points`
- `irrelevant_content`
- `time_used`
- `score_estimate`
- `review_due`

#### `essay_attempts`

- `essay_id`
- `topic`
- `outline_or_full`
- `project_fact_ids`
- `word_count`
- `time_used`
- `rubric_results`
- `factual_risks`
- `revision_history`

#### `audit_log`

记录：

- 谁调用了什么；
- 修改前后摘要；
- 成功/失败；
- 错误信息；
- 是否用户确认；
- 可回滚位置。

### 8.3 数据真相规则

- `study_events/mastery_evidence` 是原始事实；`mastery_state` 是可重新计算的派生状态。
- 不允许豆包仅凭自然语言感觉把知识点标记为“已掌握”。
- 删除或覆盖原始证据必须用户确认。
- 每次会话结束必须有 checkpoint，包含未完成动作与下一次复习。
- 所有数据结构必须提供导出为 JSON/CSV/Markdown 的能力，避免平台锁定。

---

## 9. 掌握度与错误分类

### 9.1 掌握度 0—5 级

```text
0：未学习或没有可靠证据
1：看到答案能够识别
2：脱离资料能够复述核心概念
3：能稳定解决对应选择题
4：能在案例题中识别场景并写出得分点
5：能在论文中结合真实项目进行论证、权衡与展开
```

不能因为：

- 看完视频；
- 读过 PDF；
- AI 讲解后感觉明白；
- 偶然答对一道题；

就直接提升到 3 级以上。

### 9.2 错误类型

统一使用：

```text
K：Knowledge，完全不知道
C：Confusion，概念混淆
M：Memory，学过但提取失败
A：Application，知道概念但不会应用
Q：Question reading，审题/条件遗漏
T：Time，时间管理问题
E：Expression，知道但写不成得分点
G：Guess，猜对或低置信度答对
```

低置信度答对必须记录为潜在风险，不得因“答案正确”被忽略。

### 9.3 证据权重原则

从弱到强：

```text
观看/阅读
< 开卷复述
< 闭卷复述
< 非限时选择题
< 限时选择题
< 案例得分点输出
< 论文真实展开
< 完整限时模拟
```

Codex 可以实现透明、可配置的评分函数，但不得用黑箱分数替代原始证据。默认优先级可采用：

```text
priority =
  syllabus_importance
  × weakness
  × due_factor
  × forgetting_risk
  × cross_subject_value
  × recent_error_severity
  ÷ estimated_minutes
```

所有因子需要归一化、可解释并写入调试日志。

---

## 10. 主控状态机

豆包每次训练都应遵循：

```text
OBSERVE
→ DIAGNOSE
→ PLAN
→ EXECUTE
→ TEST
→ UPDATE
→ SCHEDULE
→ CHECKPOINT
```

### 10.1 `OBSERVE`

读取：

- 用户画像；
- 考试日期；
- 到期复习；
- 最近 7/14/30 天成绩；
- 视频与资料进度；
- 综合、案例、论文投入比例；
- 上一会话未完成事项；
- 当天可用时间和精力。

### 10.2 `DIAGNOSE`

区分：

- 知识缺失；
- 概念混淆；
- 遗忘；
- 迁移到案例失败；
- 论文素材不足；
- 审题与表达问题；
- 纯时间管理问题。

### 10.3 `PLAN`

计划必须：

- 总时长不超过用户可用时间；
- 优先处理到期复习和最大通过瓶颈；
- 每项有预计时长、操作方式、完成标准；
- 不长期偏废任何科目；
- 不因课程未看完而机械按章节顺序推进；
- 精力较低时降低高负荷写作任务，而不是取消学习；
- 留出 5—10 分钟复盘与写入状态。

### 10.4 `EXECUTE`

通过技能或浏览器：

- 打开指定资料片段；
- 打开指定视频时间段；
- 打开芝士架构指定章节/年份/错题；
- 创建闭卷回忆问题；
- 启动案例或论文练习。

### 10.5 `TEST`

先让用户输出，再讲解：

- 视频后闭卷复述；
- 章节后小测；
- 错题变式提问；
- 案例按得分点批改；
- 论文按结构、事实、决策和权衡批改。

### 10.6 `UPDATE`

写入：

- 完成情况；
- 正确率与置信度；
- 错误类型；
- 新证据；
- 掌握度变化；
- 资料进度；
- 风险变化。

### 10.7 `SCHEDULE`

默认复习间隔可从 1、3、7、14、30 天开始，根据表现动态调整。错误严重、低置信度或高权重考点提前；稳定通过多种证据验证的知识点延后。

### 10.8 `CHECKPOINT`

每次会话保存：

- 已完成；
- 未完成；
- 新发现；
- 掌握度变化；
- 下一到期事项；
- 恢复所需上下文；
- 数据写入是否成功。

浏览器任务等待用户作答时，状态应为 `AWAITING_HUMAN`，不能误判为失败或直接继续给答案。

---

## 11. 核心用户流程

### 11.1 首次建档

Codex 应尽可能从已有资料和用户提供的数据预填，豆包首次运行只补充真正缺失项：

- 目标考试批次与日期；
- 两次考试各科成绩；
- 每周可用时间；
- 视频目录与已看范围；
- 已有 PDF、讲义、真题资料；
- 芝士架构账号可用状态；
- 当前综合/案例/论文自评；
- 可用于论文的真实项目经历。

首次建档不能变成长问卷。缺失信息按对计划影响排序，最多一次收集一组关键字段。

### 11.2 已看一半视频的处理

禁止从头重看。流程：

```text
导入视频目录和已看标记
→ 按已看章节做小规模诊断
→ 将错误映射到知识点
→ 定点回看薄弱时间段
→ 做对应真题/案例验证
→ 只有通过验证才标记为已转化
```

### 11.3 新视频学习

```text
观看前：给出 3—5 个问题与常见混淆点
观看中：只记时间戳、老师强调点和疑问
观看后：5 分钟闭卷复述
随后：完成对应题或案例小题
最后：更新证据与复习时间
```

视频播放完成不等于学习完成。

### 11.4 芝士架构练习

豆包应输出明确任务，例如：

```text
章节：质量属性与架构战术
模式：章节练习
题量：15
限时：20 分钟
答案策略：提交前不查看解析
需要额外记录：每题置信度
```

系统可自动打开页面和导航，但必须等待用户作答。完成后导入：

- 题量与正确率；
- 用时；
- 错题编号或可见标识；
- 低置信度正确题；
- 解析或官方出处；
- 章节统计。

导入方式优先级：

1. 芝士架构官方导出文件；
2. 结果页可见结构化信息；
3. 本项目浏览器扩展/用户脚本读取当前可见页面；
4. 截图或手工简表；
5. 用户口述结果。

严禁：

- 逆向调用未公开私有 API；
- 大规模抓取和复制题库；
- 自动选择答案、自动提交；
- 在用户作答前读取并展示答案；
- 绕过会员或访问控制。

### 11.5 案例分析训练

先由用户完整作答，豆包按 rubric 输出：

- 本问考察意图；
- 已覆盖得分点；
- 缺失得分点；
- 虽正确但不回答问题的内容；
- 表达冗余或不清晰处；
- 更短、更像考试答案的改写；
- 对应教材/课程/真题来源；
- 下次迁移练习。

不得仅给一篇标准答案覆盖用户思考过程。

### 11.6 论文训练

建立真实项目事实库，至少包括：

- 项目背景和业务目标；
- 用户角色与职责；
- 规模、约束和可确认指标；
- 总体架构；
- 关键质量属性；
- 关键技术决策；
- 候选方案与选择理由；
- 代价、风险与故障；
- 实施过程；
- 结果；
- 可映射论文主题。

豆包只能在真实事实范围内组织论文，不得虚构：

- 用户没有承担的职责；
- 不存在的规模；
- 不可确认的性能数据；
- 没有实际采用的技术；
- 与用户项目不符的行业场景。

论文训练顺序：

```text
主题识别
→ 项目事实匹配
→ 提纲
→ 局部段落
→ 完整限时成文
→ 按评分维度批改
→ 修订
→ 间隔重写
```

### 11.7 周复盘

每周自动生成：

- 三科投入时间与比例；
- 计划完成率；
- 到期复习完成率；
- 提升最快和下降最大的知识点；
- 高频错误类型；
- 限时套题趋势；
- 案例得分点缺口；
- 论文主题覆盖；
- 下周三项最高优先级；
- 应停止的低价值行为。

周报不是流水账，必须给出下一周计划调整依据。

### 11.8 冲刺与模拟

距考试日期进入阈值后自动切换：

- 减少新增长视频；
- 增加限时整套题与连续作答；
- 强化案例触发词→得分点；
- 强化论文主题提纲和机考输入；
- 汇总高频错题与低置信度正确题；
- 维持睡眠和真实考试时段适应。

具体阈值由考试日期和当前进度决定，不写死为某一天数。

---

## 12. 资料处理子系统

### 12.1 本地目录建议

```text
materials/
├── inbox/                 # 用户新放入的文件
├── raw/
│   ├── pdf/
│   ├── video/
│   ├── audio/
│   └── exports/
├── parsed/
│   ├── pdf_pages/
│   ├── transcripts/
│   └── metadata/
├── index/
├── quarantine/            # 解析失败或不可信文件
└── manifests/
```

### 12.2 PDF 流程

```text
文件发现
→ 计算哈希与去重
→ 提取元数据与目录
→ 页级文本提取
→ 仅在必要时对特定页使用 OCR
→ 按标题/段落切分
→ 映射知识点
→ 生成页码引用
→ 建立全文和可选向量索引
```

每次检索结果必须返回：

- 文件名；
- 章节；
- 页码；
- 片段；
- 可信度；
- 是否 OCR；
- 本地打开位置。

### 12.3 视频流程

```text
文件发现
→ 提取音轨
→ 语音转写
→ 标点与时间戳校正
→ 章节/主题切分
→ 映射知识点
→ 建立时间段索引
→ 生成可点击/可复制的跳转时间
```

推荐 Codex 使用本地 `ffmpeg` 加适合中文的转写方案，实际模型由设备能力和测试结果决定。字幕必须保留开始/结束时间，不能只生成一整篇文本。

搜索结果示例：

```json
{
  "resource": "05-质量属性.mp4",
  "start": "00:32:10",
  "end": "00:46:30",
  "topic": "可用性战术",
  "summary": "主动冗余、被动冗余、心跳检测",
  "open_target": "..."
}
```

### 12.4 百度网盘适配

第一版采用稳定边界：

- 用户有权下载的资料尽量下载/同步到本地；
- Codex 建立目录监听和增量导入；
- 豆包可根据索引告诉用户回看哪一段；
- 若用户继续在百度网盘播放器观看，豆包尝试打开指定课程并引导到时间点；
- 自动读取播放器进度只在实际页面测试稳定后启用；
- 登录、验证码由用户接管；
- 不依赖非公开网盘 API，不规避下载权限。

需要提供手工兜底：

```text
“我看到 05-质量属性 01:12:30”
```

系统即可更新进度，不因自动化失败阻塞学习。

### 12.5 资料来源优先级

```text
考试大纲与官方通知
> 官方教程/教材
> 经核验的历年真题和解析
> 用户购买/获得授权的辅导课程
> 国家/行业标准与官方技术文档
> 高质量网络资料
> 普通网络文章
> 模型自身记忆
```

对可能存在口径差异的知识，保留：

```text
exam_view：考试作答口径
industry_view：当前工程实践口径
```

联网资料不能静默覆盖考试口径。

---

## 13. 豆包技能设计

Codex 应根据实际 SkillHub/豆包工作版本创建技能。以下是逻辑拆分，不得在未审计前假设固定打包语法。

### 13.1 `ruankao-controller`

职责：

- 执行主状态机；
- 读取状态；
- 诊断、计划和分配任务；
- 协调其他技能；
- 结束会话并写 checkpoint；
- 管理三科平衡和冲刺模式。

### 13.2 `ruankao-materials`

职责：

- 列出资料；
- 搜索 PDF 页和视频时间段；
- 打开本地资料或百度网盘目标；
- 记录视频进度；
- 处理新增资料；
- 返回可追溯引用。

### 13.3 `cheko-practice`

职责：

- 根据主题创建练习任务；
- 打开/导航芝士架构；
- 进入等待用户状态；
- 导入官方导出、结果页、截图或手工结果；
- 记录错题与低置信度题；
- 严格阻止自动答题和提前展示解析。

### 13.4 `ruankao-assessment`

职责：

- 生成闭卷回忆问题；
- 对知识点进行分层测试；
- 错误分类；
- 生成掌握度证据；
- 安排变式练习。

### 13.5 `ruankao-case-coach`

职责：

- 维护案例题 rubric；
- 按得分点批改；
- 识别缺失、冗余、审题和表达问题；
- 生成迁移练习；
- 保存原答案与修订。

### 13.6 `ruankao-essay-coach`

职责：

- 管理真实项目事实库；
- 主题→项目素材匹配；
- 提纲与完整论文训练；
- 检查真实性、切题性、技术权衡和限时完成；
- 禁止虚构项目事实。

### 13.7 `ruankao-review-scheduler`

职责：

- 管理复习队列；
- 创建豆包/飞书定时任务；
- 每日提醒、周报和临近考试提醒；
- 避免重复提醒与过度打扰。

### 13.8 `ruankao-research-verifier`

职责：

- 搜索官方考试信息；
- 检索标准和官方技术资料；
- 对结论进行多源核验；
- 记录查询日期、来源与证据；
- 区分考试口径和工程现实。

### 13.9 `ruankao-healthcheck`

职责：

- 检查技能、连接器、状态库、资料索引、浏览器和定时任务；
- 输出 PASS/PARTIAL/FAIL；
- 提供修复建议；
- 支持一键运行部署后验收和升级回归。

### 13.10 技能开发规则

- 每个技能职责单一，避免一个超长提示词承担所有逻辑。
- 技能输入/输出使用结构化字段；自然语言只是展示层。
- 每个技能必须有错误码、失败兜底和安全边界。
- 所有写操作幂等或带唯一请求 ID。
- 技能版本化并保留回滚包。
- 上传前执行静态检查、秘密扫描和最小权限检查。
- 技能默认私有，不发布到公共商店。

---

## 14. 连接器与接口设计

### 14.1 推荐逻辑接口

不论最终由飞书多维表格还是外部服务实现，豆包应获得这些逻辑能力：

```text
get_system_health()
get_profile()
update_profile(patch)
get_dashboard(window_days)
get_due_reviews(now)
create_daily_plan(...)
record_session_event(...)
search_materials(query, filters)
open_material(target)
record_video_progress(...)
create_practice_task(...)
import_practice_result(...)
record_mastery_evidence(...)
get_topic_state(topic_id)
schedule_reviews(...)
save_case_attempt(...)
save_essay_attempt(...)
run_weekly_review(...)
finish_session(...)
export_backup(...)
```

### 14.2 接口原则

- 读写权限分离；
- 所有写操作有 `request_id`，可重复提交而不重复写入；
- 返回统一结构：`status/data/error/audit_id`；
- 校验字段类型和取值范围；
- 不允许豆包直接执行任意 SQL；
- 不允许技能传入任意本地路径；只允许已授权工作区；
- 日志不得记录密码、Cookie、验证码、API 密钥或完整敏感正文；
- 连接器密钥使用系统安全存储或环境变量，不写入 `SKILL.md` 和仓库。

### 14.3 外部状态服务兜底

若需要开发 `exam-state-service`：

- 提供 HTTPS；
- 单用户认证；
- 默认只绑定必要接口；
- 使用最小权限 token；
- 有速率限制、审计、备份、数据库迁移；
- 允许导出全部用户数据；
- 支持离线缓存后重放；
- 不把课程原始视频上传到服务；
- 定时任务可访问但不能获得超出日常运行所需的权限。

---

## 15. 豆包工作伙伴配置

### 15.1 基本信息

```text
名称：架构上岸教练
可见性：仅本人
角色：系统架构设计师考试一对一主控教练
风格：直接、证据驱动、不过度安慰、不制造虚假掌握感
语言：中文为主；保留必要英文技术名词
```

### 15.2 必备专长

- 软考系统架构设计师考试结构与备考方法；
- 软件架构、系统设计、数据库、分布式、网络、安全等知识；
- 选择题诊断；
- 案例得分点批改；
- 论文项目化写作；
- 学习科学、提取练习、间隔复习；
- 资料检索与来源核验；
- 工具编排和状态管理。

### 15.3 行为边界

- 训练前先读状态；
- 不凭聊天记忆替代数据库；
- 不在用户答题前给答案；
- 不把“看懂”当“会做”；
- 不追求把所有资料学完；
- 不因为用户焦虑就降低证据标准；
- 不编造项目、成绩、引用或考试规则；
- 不公开资料；
- 不执行付款、发布、删除等操作；
- 发现工具不可用时降级而不是假装成功。

完整系统指令见本交付包第 03 文件，Codex 应在部署时用实际技能名、连接器名和表格/服务 ID 替换占位符。

---

## 16. 定时任务设计

至少配置：

### 16.1 每日轻提醒

- 触发时间依据用户作息；
- 只提醒到期复习和当日计划；
- 无计划时引导用户输入可用时间与精力；
- 已完成则不重复催促。

### 16.2 每周复盘

- 统计过去 7 天；
- 生成结构化周报；
- 写入下周优先级；
- 检查三科失衡；
- 检查是否只看课不输出。

### 16.3 考试节点提醒

- 报名、准考证、考试日期从官方来源核验；
- 不把某地区通知直接当作所有地区规则；
- 提醒中保留来源和核验日期；
- 用户所在地区由首次建档确认。

### 16.4 失败处理

- 记录失败；
- 不因多次自动重试产生重复数据；
- 下次用户进入时显示未完成任务；
- 状态服务不可用时只做只读计划，不伪造写入成功。

---

## 17. 安全、隐私与合规

### 17.1 默认策略

- 工作伙伴、技能、资料和连接器默认私有。
- 用户个人版豆包中关闭或明确检查“帮助模型改进效果”设置，再决定是否上传敏感内容。
- 账号密码、Cookie、验证码、API 密钥不进入提示词、文档或 Git。
- 仅向豆包发送完成任务所需的文件或片段。
- 论文项目事实中的公司敏感信息使用脱敏字段。
- 课程原始资料不公开分享，不上传公共技能商店。

### 17.2 权限分级

#### 可自动执行

- 读取授权目录；
- 搜索资料；
- 读取学习状态；
- 新增普通学习事件；
- 打开指定网页；
- 生成私人计划和报告。

#### 首次授权后可执行

- 向私人飞书空间写表格/文档；
- 创建日历和定时任务；
- 调用状态连接器；
- 在授权浏览器会话中打开芝士架构或百度网盘。

#### 每次确认

- 删除/批量覆盖数据；
- 更改数据库结构；
- 对外分享、发布、发送消息；
- 付费；
- 上传大量或敏感资料；
- 更改账号与权限；
- 导出含敏感数据的完整备份。

#### 禁止

- 自动替用户完成题目；
- 在作答前读取并泄露答案；
- 绕过平台访问控制；
- 抓取或传播完整题库与课程；
- 伪造学习结果、引用或成绩；
- 静默公开数据。

---

## 18. 失败与降级策略

| 失败场景 | 首选处理 | 降级路径 |
|---|---|---|
| 豆包无法读本地目录 | 检查授权与客户端 | 通过本地桥接/HTTPS 连接器检索 |
| 百度网盘页面无法定位时间 | 尝试播放器快捷键/页面操作 | 告诉用户视频名+时间段，用户手动跳转 |
| 芝士架构 DOM 变化 | 更新选择器 | 使用截图/官方导出/手工结果 |
| 连接器暂时不可用 | 重试且保持幂等 | 本地缓存事件，恢复后重放 |
| 定时任务无法访问本地服务 | 改用云端状态/飞书表 | 只发送提醒，用户启动后再生成计划 |
| 豆包输出无来源结论 | 阻止写入掌握度 | 调用核验技能或标记“待核验” |
| 会话中断 | 读取最近 checkpoint | 从未完成动作恢复，不重做已完成任务 |
| 资料解析失败 | 隔离并记录错误 | 用户指定关键页/重新导出 PDF |
| 误写数据 | 依赖审计日志回滚 | 从每日备份恢复 |

系统设计必须保证“自动化失败不会阻止用户当天学习”。

---

## 19. 仓库结构建议

Codex 可按实际平台调整，但至少保留同等职责：

```text
doubao-ruankao-coach/
├── AGENTS.md
├── README.md
├── .env.example
├── docs/
│   ├── product-spec.md
│   ├── doubao-capability-matrix.md
│   ├── architecture.md
│   ├── security.md
│   ├── data-dictionary.md
│   ├── user-guide.md
│   ├── maintenance-guide.md
│   └── architecture-decision-records/
├── skills/
│   ├── ruankao-controller/
│   ├── ruankao-materials/
│   ├── cheko-practice/
│   ├── ruankao-assessment/
│   ├── ruankao-case-coach/
│   ├── ruankao-essay-coach/
│   ├── ruankao-review-scheduler/
│   ├── ruankao-research-verifier/
│   └── ruankao-healthcheck/
├── connectors/
├── backend/
├── schemas/
├── materials/
│   ├── inbox/
│   ├── raw/
│   ├── parsed/
│   ├── index/
│   └── manifests/
├── browser/
│   ├── cheko-extension-or-userscript/
│   └── selectors/
├── prompts/
│   ├── doubao-partner-persona.md
│   ├── doubao-bootstrap.md
│   └── recovery-prompts.md
├── deployment/
│   ├── install/
│   ├── update/
│   ├── backup/
│   └── rollback/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── e2e/
│   └── fixtures/
└── scripts/
```

Codex 必须先从当前豆包界面导出或创建一个最小技能样例，确认真实 `SKILL.md` 与文件布局，再确定 `skills/` 内最终格式；禁止凭经验编造平台协议。

---

## 20. 分阶段实施计划

### Phase 0：能力审计与架构决策

交付：

- 能力矩阵；
- 截图/日志证据；
- 运行载体决策；
- 存储和连接器决策；
- 实际技能格式样例；
- 风险清单。

通过门槛：第 7.3 节所有关键能力有可行路径。

### Phase 1：仓库、数据模型与状态服务

交付：

- 仓库初始化和 `AGENTS.md`；
- 数据表/模式；
- 状态读写 API；
- 审计、备份、迁移；
- 假数据单元测试。

### Phase 2：资料导入与检索

交付：

- PDF 页级处理；
- 视频时间戳转写；
- 增量导入；
- 检索接口；
- 本地打开能力；
- 来源定位测试。

### Phase 3：芝士架构适配

交付：

- 登录/导航流程；
- 练习任务创建；
- `AWAITING_HUMAN` 状态；
- 结果导入；
- 低置信度采集；
- DOM 变化兜底；
- 安全限制测试。

### Phase 4：学习决策引擎

交付：

- 主状态机；
- 计划生成；
- 掌握度与错误分类；
- 复习队列；
- 案例批改；
- 论文事实库和批改；
- 周报与冲刺模式。

### Phase 5：豆包技能、连接器与工作伙伴安装

交付：

- 所有技能包；
- 连接器配置；
- 私有工作伙伴；
- 系统指令；
- 定时任务；
- 权限最小化；
- 安装/更新/回滚说明。

### Phase 6：真实数据初始化

交付：

- 用户两次考试成绩录入；
- 视频目录与已看一半的进度导入；
- PDF/视频首批索引；
- 知识点地图；
- 芝士架构基线测试结果；
- 论文项目事实库第一版；
- 初始七天计划。

不得虚构用户尚未提供的数据。缺失项明确标记，按需由豆包首次运行补齐。

### Phase 7：测试与验收

执行第 22 节验收用例，修复后回归。必须用用户真实豆包、真实浏览器、真实资料样本和芝士架构实际页面测试。

### Phase 8：交付与退出

交付：

- 一条最终启动指令；
- 日常使用说明；
- 故障恢复口令；
- 健康检查；
- 数据导出与备份；
- 更新与回滚；
- 完整源代码。

Codex 确认豆包可独立完成一周模拟运行后，才视为交付完成。

---

## 21. 测试策略

### 21.1 单元测试

- 数据校验；
- 幂等写入；
- 掌握度计算；
- 复习日期；
- 优先级排序；
- 资料引用定位；
- 权限规则；
- 错误码。

### 21.2 集成测试

- 豆包技能→连接器→状态库；
- 本地资料→索引→豆包检索；
- 豆包→浏览器→芝士架构→结果导入；
- 定时任务→状态读取→报告写入；
- 连接器失败→缓存→恢复重放。

### 21.3 端到端场景

至少覆盖：

1. 用户有 90 分钟、精力一般，系统生成可执行计划。
2. 上一会话三天前中断，系统从 checkpoint 恢复。
3. 已看视频章节出现错误，系统只安排定点回看。
4. 用户在芝士架构完成 15 题，结果正确导入且不泄露答案。
5. 用户答对但低置信度，系统仍安排复习。
6. 用户提交一题案例，系统按得分点批改并记录。
7. 用户提交论文提纲，系统检查真实项目事实而不虚构。
8. 官方考试信息发生变化，系统重新核验并保留旧版本。
9. 芝士架构页面结构变化，系统切换到截图/导出兜底。
10. 状态服务断网，系统不谎报写入成功，恢复后可同步。
11. 豆包尝试在用户答题前调用解析，安全规则阻止。
12. 定时周报只读数据并成功生成，不重复写入。
13. 用户要求删除全部状态，系统要求明确确认并先导出备份。
14. 新增 PDF/视频后，增量导入不重复处理已有文件。
15. 运行健康检查后能准确指出断开的能力。

### 21.4 回归测试

每次豆包客户端、技能、连接器、浏览器扩展或数据模式更新后，必须运行核心回归套件。

---

## 22. 验收标准 / Definition of Done

系统只有同时满足以下条件才算完成：

### 豆包运行层

- [ ] 用户当前账号中存在私有“架构上岸教练”。
- [ ] 工作伙伴已写入最终系统指令。
- [ ] 所需技能和连接器均可见、可调用、权限正确。
- [ ] 一条启动指令可进入完整主控流程。

### 状态与学习闭环

- [ ] 每次开始前读取真实状态。
- [ ] 每次结束后写入事件、掌握度、复习队列和 checkpoint。
- [ ] 关闭并重新打开豆包后仍可恢复。
- [ ] 定时任务与日常对话读取同一状态。

### 资料

- [ ] PDF 检索能定位到页码。
- [ ] 视频检索能定位到时间段。
- [ ] 已看一半的视频进度已导入，不从头重学。
- [ ] 搜索结果区分可靠来源和待核验内容。

### 芝士架构

- [ ] 豆包能创建任务并打开正确页面或给出明确操作。
- [ ] 用户提交前不展示答案。
- [ ] 结果可通过至少一种稳定方式导入。
- [ ] 错题与低置信度正确题都会进入学习证据。
- [ ] 不使用私有 API 逆向或复制整套题库。

### 三科训练

- [ ] 综合、案例、论文均有独立证据和计划。
- [ ] 案例按得分点批改。
- [ ] 论文只使用真实项目事实。
- [ ] 周报能发现三科失衡。

### 安全与恢复

- [ ] 密钥和登录信息不在仓库、提示词和日志中。
- [ ] 高风险动作要求确认。
- [ ] 有数据导出、备份、恢复和回滚流程。
- [ ] 工具失败时不假装成功。
- [ ] 自动化失败时用户仍可继续当天学习。

### 工程交付

- [ ] 源代码、配置、文档、测试完整。
- [ ] `healthcheck` 通过。
- [ ] 安装、更新和回滚有脚本或清晰步骤。
- [ ] 能在不依赖 Codex 的情况下连续模拟运行至少 7 天。

---

## 23. Codex 必须避免的错误

- 只写一个很长的豆包提示词，未构建状态、技能和工具。
- 未测试豆包实际能力就选技术方案。
- 把豆包当开发者，让豆包自己边用边搭。
- 建一个新的刷题系统替代芝士架构。
- 重新复制或抓取完整题库。
- 把课程全部转成笔记，却没有测试闭环。
- 把“视频看完”当作掌握。
- 只准备综合知识，拖延案例和论文。
- 让 AI 提前给题目答案。
- 用聊天记录充当数据库。
- 将本地 SQLite 设计成云端定时任务无法访问的唯一状态源。
- 把课程、公司项目事实或账号信息上传到公共空间。
- 声称浏览器自动化成功却没有真实页面测试。
- 使用不可维护的硬编码选择器而没有截图/导出兜底。
- 只交付 Demo，不交付备份、恢复、健康检查和验收报告。

---

## 24. 最终交付物清单

Codex 最终必须交付：

1. 完整源代码仓库；
2. 豆包能力审计矩阵和证据；
3. 架构与数据字典；
4. 私有豆包工作伙伴；
5. 可安装和可回滚的技能包；
6. 连接器与状态服务；
7. PDF/视频处理与索引工具；
8. 芝士架构浏览器适配和兜底流程；
9. 初始资料、进度与历史成绩导入；
10. 案例和论文训练模板；
11. 定时提醒与周报；
12. 测试代码、测试记录与验收报告；
13. 安装、使用、维护、备份、恢复和升级文档；
14. 最终豆包系统指令；
15. 用户每日的一条启动口令；
16. 故障时的恢复口令；
17. 7 天独立模拟运行记录。

---

## 25. 当前公开能力依据与需实测项

截至 2026-09-03，公开资料支持以下设计判断：

- 豆包工作被定位为可以理解目标、规划步骤、搜索/读取信息、调用工具并交付成果的 Agent 平台。
- 豆包工作支持在授权范围内读取公开网页、本地文件或企业知识，并可操作浏览器和部分电脑界面。
- 豆包工作公开说明包含自定义技能、导入本地技能文件、自定义连接器、工作伙伴、定时任务和飞书办公能力。
- SkillHub 公开说明允许通过上传、对话生成或编辑 `SKILL.md` 创建技能，并将技能安装到 Agent。
- 芝士架构公开说明包含历年真题、选择/案例/论文分析、预测分、错题筛选与 PDF 导出。
- 2026 年官方考试通知显示系统架构设计师采用计算机化考试，且参加考试人员需一次通过全部应试科目。

但以下内容必须由 Codex 在用户账号上实测，不能从公开资料直接推断：

- 当前个人账号是否已开放全部工作伙伴、技能和连接器能力；
- 自定义技能的精确文件格式和脚本执行限制；
- 自定义连接器是否可访问本机或所选 HTTPS 服务；
- 定时任务能否访问同一状态；
- 百度网盘与芝士架构页面的实际浏览器可操作程度；
- 当前权益下的文件大小、任务时长和调用限制。

### 参考资料

- 飞书官网：《豆包工作全新发布！详解核心价值、主要功能，以及与豆包、飞书的关系》，2026-08-24：  
  https://www.feishu.cn/content/article/7677519271848610746
- 飞书官网：《飞书 aily SkillHub 使用指南》，2026-06-02：  
  https://www.feishu.cn/content/article/7646699294103292898
- 豆包隐私政策：  
  https://www.doubao.com/legal/privacy
- 芝士架构功能说明：  
  https://docs.cheko.cc/app/web_about.html
- 深圳市考试院：《关于2026年度计算机技术与软件专业技术资格（水平）考试有关事项的通知》：  
  https://hrss.sz.gov.cn/szksy/zwgk/bmxx/content/post_12693780.html

---

## 26. 最终产品定义

最终产品不是“豆包帮我回答软考问题”，而是：

```text
一个由 Codex 开发和部署、以豆包工作伙伴为唯一主控、
能持续读取外部状态、调用本地资料和网页工具、
监督用户真实学习与答题、根据证据动态调整计划、
并覆盖综合知识/案例/论文完整闭环的个人备考系统。
```

用户在部署后无需理解内部技术，只需通过豆包开始每天的训练；系统是否成功，以真实学习闭环和可恢复性为准，而不是以生成了多少提示词、笔记或页面为准。
