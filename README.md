# ArchitectPass / 架构上岸教练

这是供豆包从公开链接部署的正式交付分支。新用户无需安装 Git、配置 SSH 或注册 GitHub。日常学习主控固定为豆包；芝士架构继续负责做题，百度网盘继续提供用户有权使用的资料，长期状态保存在用户私有的飞书多维表格中。

收到压缩包的新用户：先解压，再执行包根的 `README-OFFLINE.md` 和 `deployment/offline/bootstrap-v1.md`；该路径不需要 Git 仓库或百度网盘。首次档案为空，资料索引用 `scripts/prepare_offline_materials.py` 在当前电脑建立。原始基线中的个人经历不是接收者的学习数据。

## 豆包一键初始化入口（必要时需本人点击）

公开项目地址（main 分支）：

```text
https://github.com/chichu-lv/ruankao_doubao.git
```

在豆包桌面客户端新建工作任务，确认选择「本地电脑」，再发送上面的链接和下面整段提示词。网页版的「云电脑」不能读取本机资料包。首次账号登录和系统权限弹窗仍需本人完成，后续由豆包执行部署：

```text
你就是当前执行部署的豆包，不是 Codex，不要打开另一个豆包或在虚拟桌面登录豆包。未明确要求恢复时，按首次安装直接下载到独立新目录；不要扫描主目录、Downloads、开发仓库或旧交付包寻找历史部署，不读取偶然发现的开发/QA资料。先确认当前工具操作的是我这台电脑；如果只有云电脑或无法确认，请停止并指导我在豆包桌面客户端新建“本地电脑”任务。请从公开仓库 https://github.com/chichu-lv/ruankao_doubao 的 main 部署“架构上岸教练”，先读 deployment/doubao/execution-context-v1.md，再按 README.md 和 deployment/doubao/bootstrap-v1.md 执行。我只会使用豆包，请自动下载源码 ZIP、准备项目内环境，不要求我安装 Git、配置 SSH 或手输命令。按你当前官方“创建技能”指南和 deployment/doubao/folder-skills-v1.md，优先自动安装九个技能文件夹并绑定本机项目位置；只有官方方式不可用时才让我完成具体点击，不要求我找不存在的项目指令字段，不转去网页豆包或虚拟桌面。本机百度网盘客户端已安装并登录，请在资料步骤核对两个授权课程目录，按需下载索引。连接我自己的飞书和芝士架构；仅对本次目标目录或官方九技能安装绑定确认的本次部署先核对后继续，只有首次无档案时才创建独立空档案，来源不明先询问而不扩大搜索。安装所需项目内操作及这九个官方技能目录的新增写入已授权；第三方登录和系统授权由我本人完成。不继承资料提供者进度，不修改其它项目，不重做 Codex 的开发或能力审计。分别报告本机持久入口与原生侧边栏项目的真实状态，完成真实健康检查后开始训练。
若为 Mac，在当前用户 Documents/ArchitectPass 下选择独立新子目录作为持久安装根，不能用豆包会话的 .sessions/scratch 临时目录运行项目；源码 ZIP 用 Python 标准库 zipfile 解压，不使用交互式 unzip。停止时只停止本轮任务或已核实的进程 ID，不按通用进程名批量终止。
```

使用路径：安装并登录豆包桌面客户端 → 新工作任务选择「本地电脑」→ 发送上述提示词 → 按引导完成本人登录/授权。优先使用豆包官方文件夹技能机制；不支持时才回退到必要的自身界面点击，不能改用虚拟桌面再登录豆包。本机持久技能入口不等于账号云端技能或原生侧边栏项目。这里不是“纯提示词、零点击部署已通过”的承诺；新的安装路径仍须通过完整新用户实测。Windows 授权界面的逐屏引导仍以当前实际界面为准。

已卡在虚拟桌面登录豆包：[中断恢复提示词](deployment/doubao/execution-context-v1.md#给卡住用户的恢复提示词)。不要清空旧安装，不要复制虚拟机路径冒充本机路径；先确认本地模式，再核对已完成步骤继续。旧压缩包和第三方镜像不会自动更新，恢复时也可直接粘贴该提示词。

网盘仅使用以下两个目录，共用资料账号也不继承资料提供者的学习进度。不要把课程原文件、转写正文或私有索引提交到 Git：

- `00、【推荐】【26年10月】wen老师架构课程（第二版）`
- `5、【2026年05月】芝士架构系统架构设计师`

Windows 自动下载入口是 [scripts/install_public_windows.ps1](scripts/install_public_windows.ps1)，由豆包执行，不要求用户打开终端。先用 `-FetchOnly` 获取 main 源码 ZIP，完整阅读基线后再运行该入口准备环境。默认目录为当前用户的 `%LOCALAPPDATA%\ArchitectPass`，无需管理员权限、系统 Python 或 Git；已存在未知目录时不覆盖，由豆包选择新的项目子目录。详细命令见自举协议。

## 豆包必须执行的入口

[deployment/doubao/bootstrap-v1.md](deployment/doubao/bootstrap-v1.md) 是唯一安装状态机。豆包必须从当前源码 ZIP 解压目录（或已有克隆目录）解析项目根路径，不得使用开发电脑的历史绝对路径，也不得把父目录、用户主目录或磁盘根目录加入权限范围。

正式安装会：

1. 校验 main 源码 ZIP 的下载来源记录、`VERSION` 和必需文件；源码包没有 `.git` 是正常情况；
2. Windows 先运行 `scripts/download_windows_runtime.ps1` 获取项目私有环境，再运行 `scripts\start_windows.cmd`；Mac/Linux 运行 `python3 scripts/bootstrap_local.py`。安装依赖并构建九个私有豆包技能包；完整离线包不需要下载运行环境；
3. 建立本次私有本机工作区，按当前官方指南自动安装九个技能文件夹；原生侧边栏项目单独核实，未创建不得谎报；
4. 各技能通过本机安装绑定读取 `deployment/doubao/system-instructions-v1.md`；新任务实际验证不提供路径也能找到绑定；
5. 按 `schemas/feishu-bitable-v1.json` 连接或初始化私有 `ArchitectPass State v1`；
6. 优先使用已安装、已登录的 Windows 百度网盘官方客户端，仅核对两个授权课程目录，再按需下载并建立本机索引；客户端登录不等于浏览器登录，不强制重复网页登录；
7. 幂等核对初始化数据，保留本地资料和芝士架构的安全边界；
8. 按当前用户作息配置只读提醒，周六 20:00 可作为推荐时间；
9. 调用 `ruankao-healthcheck-v1` 并按 `PASS/PARTIAL/FAIL` 如实报告。

## 交付内容

- `skills/doubao/`：九个版本化私有技能；
- `deployment/doubao/`：豆包项目、自举、系统指令和只读任务配置；
- `deployment/feishu/` 与 `schemas/`：私有状态层定位和数据契约；
- `backend/`：状态、资料、芝士结果、学习控制与初始化逻辑；
- `materials/manifests/`：用户已授权资料的私有清单；
- `scripts/`：构建、初始化与健康检查入口；
- `tests/`：部署前本地安全和回归检查。
- `deployment/offline/`：GitHub 或百度网盘不可访问时的私有离线压缩包方案。

产品和验收基线 `01_豆包软考私教系统_Codex开发说明书.md`、`02_交给Codex的总执行指令.md`、`03_豆包工作伙伴_最终系统指令模板.md`、`04_验收清单.md` 必须保留并在自举时读取。开发过程、审计截图、阶段日志和历史测试报告仅保存在 `development` 分支，不属于正式部署输入。

## 更新与维护

同一任务中可以直接说「继续上次训练」。换新任务时仍需选择「本地电脑」；如果当前豆包没有项目级文件/指令绑定入口，请使用：

```text
从【实际项目根目录：公开源码安装默认 %LOCALAPPDATA%\ArchitectPass；完整资料包使用包内 project 目录】恢复架构上岸教练，先读取 deployment/doubao/system-instructions-v1.md 和 dist/deployment/project-state.json，再回读最近检查点继续训练。
```

目录是新会话找到本机持久配置的入口，不需要重复提供飞书标识、历史成绩或学习进度。只在已验证新会话能自动找到绑定文件时，才可以省略目录。

正式版本见 `VERSION`。重复运行安装入口只恢复原快照，不自动覆盖升级；需要更新时按 `deployment/update/README.md` 执行，不要求用户手动操作 Git。任何删除、发布、权限变化、批量覆盖或敏感数据导出仍需用户对精确目标单独确认。

需要提前制作离线备份时，先把两个授权百度网盘目录通过官方界面下载到同一父目录，再按 `deployment/offline/README.md` 生成不进入 Git 的私有 ZIP。压缩包内置正式项目快照、九个预构建技能包和实际纳入的资料清单；缺文件时只能生成明确标注的 `PARTIAL` 包。
